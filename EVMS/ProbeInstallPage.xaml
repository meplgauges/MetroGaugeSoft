<UserControl x:Class="EVMS.ProbeInstallPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:EVMS"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1100">

    <UserControl.Resources>
        <!-- Colors -->
        <SolidColorBrush x:Key="PrimaryColor" Color="#2563EB"/>
        <SolidColorBrush x:Key="PrimaryDark" Color="#1D4ED8"/>
        <SolidColorBrush x:Key="AccentColor" Color="#DC2626"/>
        <SolidColorBrush x:Key="SurfaceColor" Color="#F8FAFC"/>
        <SolidColorBrush x:Key="CardColor" Color="White"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#0F172A"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#64748B"/>
        <SolidColorBrush x:Key="SuccessColor" Color="#059669"/>

        <!-- Shadows -->
        <DropShadowEffect x:Key="CardShadow" Color="#000000" Direction="270"
                          ShadowDepth="4" BlurRadius="16" Opacity="0.12"/>
        <DropShadowEffect x:Key="HeaderShadow" Color="#000000" Direction="270"
                          ShadowDepth="2" BlurRadius="8" Opacity="0.08"/>

        <!-- Modern Button -->
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="Margin" Value="8,0,0,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="44"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="12"
                                Effect="{StaticResource CardShadow}"
                                RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <ScaleTransform ScaleX="1" ScaleY="1"/>
                            </Border.RenderTransform>
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource PrimaryDark}"/>
                                <Setter TargetName="Border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#000000" Direction="270" ShadowDepth="6" BlurRadius="20" Opacity="0.18"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="Border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="1.02" ScaleY="1.02"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource PrimaryDark}"/>
                                <Setter TargetName="Border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AccentButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernButtonStyle}">
            <Setter Property="Background" Value="{StaticResource AccentColor}"/>
        </Style>

        <Style x:Key="SuccessButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernButtonStyle}">
            <Setter Property="Background" Value="{StaticResource SuccessColor}"/>
        </Style>

        <!-- Enhanced DataGrid Styles -->
        <Style x:Key="ModernDataGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="{StaticResource CardColor}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#E2E8F0"/>
            <Setter Property="RowBackground" Value="{StaticResource CardColor}"/>
            <Setter Property="AlternatingRowBackground" Value="#F8FAFC"/>
            <Setter Property="ScrollViewer.Background" Value="{StaticResource CardColor}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="RowHeight" Value="52"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="IsReadOnly" Value="True"/>
        </Style>

        <Style x:Key="CenterHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#3B82F6" Offset="0"/>
                        <GradientStop Color="#1D4ED8" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>

        <Style x:Key="CenteredCellStyle" TargetType="DataGridCell">
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="TextBlock.TextAlignment" Value="Center"/>
            <Setter Property="TextBlock.VerticalAlignment" Value="Center"/>
            <Setter Property="TextBlock.FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#3B82F6"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ComboBox Style -->
        <Style x:Key="ModernComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource CardColor}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="44"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" 
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          Background="{TemplateBinding Background}"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Grid>
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="8"/>
                                            <Path x:Name="Arrow" 
                                                  Fill="{StaticResource TextSecondary}"
                                                  HorizontalAlignment="Right"
                                                  VerticalAlignment="Center"
                                                  Margin="0,0,12,0"
                                                  Data="M6,9L10,13L14,9"/>
                                        </Grid>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite"
                                              IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="12,0,30,0"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"/>
                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Border Background="{StaticResource CardColor}"
                                        BorderBrush="#D1D5DB"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                        Effect="{StaticResource CardShadow}">
                                    <ScrollViewer Margin="0,4" 
                                                  SnapsToDevicePixels="True">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#3B82F6"/>
                            </Trigger>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter Property="BorderBrush" Value="#3B82F6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="{StaticResource SurfaceColor}"
            CornerRadius="20"
            Padding="24"
            Effect="{StaticResource CardShadow}" >
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Header -->
                <RowDefinition Height="0"/>
                <!-- Spacer -->
                <RowDefinition Height="Auto"/>
                <!-- Assignment Panel -->
                <RowDefinition Height="24"/>
                <!-- Spacer -->
                <RowDefinition Height="*"/>
                <!-- DataGrid -->
                <RowDefinition Height="Auto"/>
                <!-- Footer -->
            </Grid.RowDefinitions>

            <!-- HEADER -->
            

            <!-- ASSIGNMENT PANEL -->
            <Border Grid.Row="2" 
                    Background="{StaticResource PageBackgroundBrush}"
                    CornerRadius="20" 
                    Padding="15" 
                    Margin="0,0,0,0" 
                    Effect="{StaticResource CardShadow}">
                <StackPanel>
                    <TextBlock Text="➕ Add New Probe Mapping" 
                               FontSize="24" 
                               FontWeight="Bold" 
                               Foreground="{StaticResource TextPrimary}"
                               Margin="0,0,0,24"/>

                    <UniformGrid Columns="5" VerticalAlignment="Center">
                        <!-- Parameter -->
                        <StackPanel Margin="0,0,24,0">
                            <TextBlock Text="📋 Parameter" 
                                       FontSize="14" 
                                       FontWeight="SemiBold" 
                                       Foreground="{StaticResource TextPrimary}"
                                       Margin="0,0,0,10"/>
                            <ComboBox ItemsSource="{Binding Parameters}"
                                      DisplayMemberPath="Name"
                                      SelectedItem="{Binding SelectedParameter, Mode=TwoWay}"
                                      Style="{StaticResource ModernComboBoxStyle}"/>
                        </StackPanel>

                        <!-- Box -->
                        <StackPanel Margin="0,0,24,0">
                            <TextBlock Text="📦 Box" 
                                       FontSize="14" 
                                       FontWeight="SemiBold" 
                                       Foreground="{StaticResource TextPrimary}"
                                       Margin="0,0,0,10"/>
                            <ComboBox ItemsSource="{Binding AvailableBoxes}"
                                      SelectedItem="{Binding SelectedBox, Mode=TwoWay}"
                                      Style="{StaticResource ModernComboBoxStyle}"/>
                        </StackPanel>

                        <!-- Channel -->
                        <StackPanel Margin="0,0,24,0">
                            <TextBlock Text="🔗 Channel" 
                                       FontSize="14" 
                                       FontWeight="SemiBold" 
                                       Foreground="{StaticResource TextPrimary}"
                                       Margin="0,0,0,10"/>
                            <ComboBox ItemsSource="{Binding AvailableChannels}"
                                      SelectedItem="{Binding SelectedChannel, Mode=TwoWay}"
                                      Style="{StaticResource ModernComboBoxStyle}"/>
                        </StackPanel>

                        <!-- Probe Name -->
                        <StackPanel Margin="0,0,24,0">
                            <TextBlock Text="🎯 Probe Name" 
                                       FontSize="14" 
                                       FontWeight="SemiBold" 
                                       Foreground="{StaticResource TextPrimary}"
                                       Margin="0,0,0,10"/>
                            <ComboBox ItemsSource="{Binding ProbeNames}"
                                      SelectedItem="{Binding SelectedProbeName, Mode=TwoWay}"
                                      Style="{StaticResource ModernComboBoxStyle}"/>
                        </StackPanel>

                        <!-- Add Button -->
                        <StackPanel VerticalAlignment="Bottom">
                            <Button Content="✔ Add Mapping" 
                                    Style="{StaticResource ModernButtonStyle}" 
                                    Click="BtnAddMapping_Click"/>
                        </StackPanel>
                    </UniformGrid>
                </StackPanel>
            </Border>

            <!-- DATAGRID -->
            <Border Grid.Row="4" 
                    Background="{StaticResource PageBackgroundBrush}"
                    CornerRadius="20" 
                    Padding="0" 
                    Effect="{StaticResource CardShadow}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- DataGrid Header -->
                    <Border Grid.Row="0" 
                            Background="{StaticResource CardColor}" 

                            CornerRadius="20,20,0,0" 
                            Padding="28,28,28,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="📋 Probe Mappings" 
                                           FontSize="22" 
                                           FontWeight="Bold" 
                                           Foreground="{StaticResource TextPrimary}"
                                           FontFamily="Segoe UI"/>
                                <TextBlock Text="{Binding Mappings.Count, StringFormat='{}Total: {0} mappings configured'}" 
                                           FontSize="15" 
                                           Foreground="{StaticResource TextSecondary}"
                                           Margin="0,4,0,0"/>
                            </StackPanel>

                            <Button Grid.Column="1" 
                                    Content="💾 Save All Mappings" 
                                    Height="48" 
                                    MinWidth="240" 
                                    Margin="0,0,0,16"
                                    Style="{StaticResource SuccessButtonStyle}" 
                                    Click="BtnSaveMappingsToDb_Click"/>
                        </Grid>
                    </Border>

                    <DataGrid Grid.Row="1" 
                              ItemsSource="{Binding Mappings}" 
                              AutoGenerateColumns="False"
                              Style="{StaticResource ModernDataGrid}"
                              CellStyle="{StaticResource CenteredCellStyle}"
                              ColumnHeaderStyle="{StaticResource CenterHeaderStyle}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Parameter" 
                                                Binding="{Binding Parameter}" 
                                                Width="2*"/>
                            <DataGridTextColumn Header="📦 Box" 
                                                Binding="{Binding BoxId}" 
                                                Width="140"/>
                            <DataGridTextColumn Header="🔗 Channel" 
                                                Binding="{Binding ChannelId}" 
                                                Width="140"/>
                            <DataGridTextColumn Header="🎯 Probe Name" 
                                                Binding="{Binding ProbeName}" 
                                                Width="2*"/>

                            <DataGridTemplateColumn Header="⚙️ Actions" Width="160">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="🗑️ Delete" 
                                                Width="120" 
                                                Height="40" 
                                                Margin="2"
                                                Background="{StaticResource AccentColor}"
                                                Foreground="White"
                                                BorderThickness="0"
                                                FontWeight="SemiBold"
                                                FontSize="13"
                                                Style="{StaticResource ModernButtonStyle}"
                                                Click="BtnDeleteMapping_Click"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

           
        </Grid>
    </Border>
</UserControl>
